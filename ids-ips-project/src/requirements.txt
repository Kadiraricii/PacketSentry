# PacketSentry IDS/IPS Projesi için Gerekli Kütüphaneler

# Temel paket yakalama ve ağ analizi için
scapy>=2.4.5

# Sistem bilgileri ve arayüz durumu kontrolü için
psutil>=5.9.0

# HTTP bağlantı testi ve Tehdit İstihbaratı API'si için
requests>=2.28.1

# Tablo formatında çıktı almak için
tabulate>=0.9.0

# Anormal davranış tespiti için (gelecekteki makine öğrenimi entegrasyonu için)
# scikit-learn==1.0.2

# Diğer bağımlılıklar (standart kütüphaneler hariç tutulmuştur)
# threading, queue, json, subprocess, collections, time, functools gibi standart kütüphaneler için bağımlılık belirtilmemiştir.